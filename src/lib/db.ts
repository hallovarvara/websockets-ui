import { PlayerCredentialsT } from './types/player.type';
import { RoomT } from './types/room.type';
import { GameT } from './types/game.type';
import { WinnerT } from '@/lib/types/winner.type';

export const users: PlayerCredentialsT[] = [];

export const rooms: RoomT[] = [];

export const games: GameT[] = [];

export const gamesSinglePlayer: GameT[] = [];

export const winners: WinnerT[] = [];
